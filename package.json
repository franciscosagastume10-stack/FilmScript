{
  "name": "filmscript-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env server.js",
    "start": "node server.js",
    "build:netlify": "node scripts/build-netlify.mjs",
    "build:vercel": "node scripts/build-netlify.mjs",
    "recurrente:webhook": "node --env-file=.env scripts/register-recurrente-webhook.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22 <25"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@aws-sdk/client-s3": "^3.1087.0",
    "better-sqlite3": "^12.11.1"
  }
}
