import requestHandler from "../server.js";

// Vercel's Node.js runtime accepts the same request/response contract as the
// native Node HTTP server used by FilmScript locally.
export default requestHandler;
